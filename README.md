This project classifies emails using both BERT and Torch to determine if an is a phishing emails. Active learning is implemented to update the model in learning any uncertain emails. Performance is compared with other models to determine the success of the model. 

Preprocessing is done through BERT and other models are processed through NLTK.
